## 問題1

次のプログラムを見て、表示される結果を答えなさい。

```java
int[] nums = {10, 20, 30, 40, 50};

System.out.println(nums[2]);
```

## 問題2
次のプログラムを見て、表示される結果を答えなさい。

```
String[] fruits = {
    "りんご",
    "みかん",
    "ぶどう",
    "いちご"
};

System.out.println(fruits[0]);
System.out.println(fruits[3]);
```


## 問題3
次のプログラムを見て、表示される結果を答えなさい。
```java
int[] scores = {70, 80, 90, 100};

int index = 1;

System.out.println(scores[index + 1]);
```

## 問題4
次のプログラムを見て、表示される結果を答えなさい。
```java
char[] chars = {'A', 'B', 'C', 'D'};

System.out.println(chars.length);
```

## 問題5
次のプログラムを見て、表示される結果を答えなさい。
```java
int[] values = {5, 10, 15};

values[1] = 20;

System.out.println(values[1]);
```

## 問題6
次のプログラムを見て、表示される結果を答えなさい。
```java
int[] nums = {1, 2, 3, 4, 5};

int sum = nums[0] + nums[2] + nums[4];

System.out.println(sum);
```


## 問題7
int型の配列 values を宣言し、10, 20, 30, 40 を格納した状態で初期化してください。
配列valuesのすべての要素を拡張for文を使って表示してください。
（作成したプログラムを張り付けてください。）


## 問題８

次のプログラムを見て、表示される結果を答えなさい。

```java

int[] days = {0, 1, 2, 3, 4, 5, 6};

String[] weekNames = {
    "日曜日",
    "月曜日",
    "火曜日",
    "水曜日",
    "木曜日",
    "金曜日",
    "土曜日"
};

System.out.println("------曜日一覧------");

for (int i = 0; i < days.length; i++) {
    System.out.println(weekNames[days[i]]);
}
```

## 問題9

次のプログラムを見て、表示される結果を答えなさい。

```java
int[] lights = {0, 1, 2, 0, 2};

String[] lightNames = {
    "赤",
    "黄",
    "青"
};

System.out.println("------信号状態------");

for (int i = 0; i < lights.length; i++) {
    System.out.println(lightNames[lights[i]]);
}
```

## 問題10
次の配列の中で、80点以上の人数を数えるプログラムを作成してください
（作成したプログラムを張り付けてください。）


int[] scores = {60, 85, 90, 70, 80, 55, 100};